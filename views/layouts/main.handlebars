<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>User Registry</title>
  <link rel="stylesheet" href="/css/jass.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-Zenh87qX5JnK2Jl0vWa8Ck2rdkQ2Bzep5IDxbcnCeuOxjzrPF/et3URy9Bv1WTRi" crossorigin="anonymous">
  <link rel="stylesheet" href="/css/style.css">
</head>

<body>


  {{!-- this if-statement is as confusing to read as it was to understand not having the login/signup buttons appear on certain pages --}}
    {{#if doNotShowButtons}}
    {{else}}

      {{#if loggedIn}}
      {{!-- these buttons will appear when logged in --}}
        <div id="userName" class="d-flex justify-content-between mx-2">
          <button id="highscore">Highscores</button>

          {{#if gamePage}}
          <p class="fs-2 text-white">Good luck, {{user.name}}!</p>
          {{/if}}
          <button id="logOut">Logout</button>
        </div>      
      {{else}}
      {{!-- these buttons will appear when not logged in --}}
        <div class="col-md-6">
          <button id="login">Login</button>
          <button id="signup">Sign Up</button>
        </div>
      {{/if}}
    {{/if}}
    

      {{{body}}}
</body>
<script src="/js/score.js"></script>
<script src="/js/redirects.js"></script>
<script src="/js/logout.js"></script>

</html>





